/**
 *  TS EXAMPLE.
 *  This is a template file for a simple ts.
 */

/**
 * We are using typescript.
 */
const getGreeting = (name: string): string => {
  return `Hello ${name}`;
};

export = getGreeting;
